@model Employee

@{
    ViewData["Title"] = "Employee Details";
}

<h1>@ViewData["Title"]</h1>
<br />

<h2 style="color: @ViewBag.Color">@Model.Name</h2>
<h2>@ViewData["Message"]</h2>
<h2>@(ViewBag.Temp - 10)</h2>

<h2>
    <select>
        @foreach (var branch in ViewData["Branches"] as IEnumerable<string>)
        {
            <option>@branch</option>
        }
    </select>

</h2>

@* <h2>@(string.Join(" - ", ViewData["Branches"] as IEnumerable<string>))</h2> *@
@*
<h2>
    @foreach (var branch in ViewData["Branches"] as IEnumerable<string>)
    {
        @branch
        <br />
    }
</h2> *@